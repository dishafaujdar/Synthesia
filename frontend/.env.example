# Backend API URL (used for research API calls and /api rewrites).
# Local: backend runs on port 3001.
# Production (e.g. Render): set to your backend URL, e.g. https://synthesia-buq6.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:3001
